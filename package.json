{
  "name": "foundation-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ adpe/foundation-subgraph",
    "create-local": "graph create --node http://localhost:8020/ adpe/foundation-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ adpe/foundation-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 adpe/foundation-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.1",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
